import { NoSpacesDirective } from './only-letters.directive';

describe('NoSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
